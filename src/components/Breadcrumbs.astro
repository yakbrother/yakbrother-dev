---
interface Props {
  path: string[];
}

const { path } = Astro.props;

function ucFirst(str: string) {
  return str.charAt(0).toUpperCase() + str.slice(1);
}
---

<nav aria-label="Breadcrumb" class="breadcrumbs">
  <ol>
    <li><a href="/">Home</a></li>
    {path.map((item, index) => (
      <li>
        {index === path.length - 1 ? (
          <span aria-current="page">{ucFirst(item)}</span>
        ) : (
          <a href={`/${path.slice(0, index + 1).join('/')}`}>{ucFirst(item)}</a>
        )}
      </li>
    ))}
  </ol>
</nav>

<style>
  .breadcrumbs {
    margin: 1rem 0 2rem;
    padding-left: 0; /* ensure no left indent */
    font-size: var(--text-size-meta);
    color: var(--color-mid);
  }

  .breadcrumbs ol {
    list-style: none;
    padding: 0;
    margin: 0;
    padding-left: 0;
    margin-left: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .breadcrumbs li {
    display: flex;
    align-items: center;
  }

  .breadcrumbs li:not(:last-child)::after {
    content: "/";
    margin-left: 0.5rem;
    color: var(--color-mid);
  }

  .breadcrumbs a {
    color: inherit;
    text-decoration: none;
    transition: color 0.2s;
  }

  .breadcrumbs a:hover {
    color: var(--color-global-link-color);
    text-decoration: underline;
  }

  .breadcrumbs [aria-current="page"] {
    color: inherit;
    font-weight: 500;
  }
</style>
